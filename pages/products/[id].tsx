import { useQuery } from "react-query";
import { QueryKeys } from "../../utils/fetcher";

const ProductDetail = () => {
//   const { data } = useQuery<Product>([QueryKeys.PRODUCTS, id], () =>
//   fetcher({ method: "GET", path: "/products" })
// );
  return <div>상세페이지</div>;
};

export default ProductDetail;
